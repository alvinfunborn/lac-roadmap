# Roadmap 数据审计记录

日期：2026-04-15
范围：`D:/workspace/projects/lifeset/lacob/LaC/Roadmap/*.md`
执行者：Wave 1 数据管家（自动）

## 一、已自动修复

### 1. 重复 Wikilink（必做项 #1）
- `日本之行.md`：第 16、17 行连续出现两次 `[[秋叶原]]`，已删除第 17 行那一份。保留后续 `route = {...}` 行，关联依然是 `[[银座]] -> [[秋叶原]]`。

### 2. `type` / `renders` 规范化（必做项 #2）
按设计约定：顶级路线（被 `roadmap.md` 引用）**不需要** `type` / `renders`；仅根入口（`roadmap.md` → `roadmapset`）和递归子路线入口（如 `银座.md` → `roadmap`）需要。

已从以下顶级路线文件中删除多余的 `type = "root"` 与 `renders = ["roadmap"]`：

- `丽江大理昆明.md`
- `北京一日.md`
- `夸郎坡.md`
- `成都美食两日.md`
- `杭州西湖一日.md`
- `访问成都.md`
- `青甘大环线.md`

保持不变：

- `roadmap.md`：`type = "root"` + `renders = ["roadmapset"]`（根入口，正确）。
- `银座.md`：`type = "root"` + `renders = ["roadmap"]`。它在 `日本之行.md` 中被当作子地点引用（`[[银座]]`），自身又包含 `[[银座药局]]` / `[[银座奶茶]]` 两个子项，属于**递归子路线入口**，保留。

未带 `type`/`renders` 的其他顶级路线（`上海外滩一日.md`、`日本之行.md`、`京都奈良两日.md`、`未来春游.md`、`未计划周末.md`）本身就符合规范，未改动。

其他纯地点（叶子）文件本身就没有 `type`/`renders`，符合规范。

### 3. 日期格式修复（必做项 #4 的一部分）
- `随机.md`
  - `start_time = "2026-01-30 22:24"` → `start_time = "2026-01-30 22:24:00"`（补齐秒）
  - `end_time = ""` → 整行删除（空字符串会干扰 TOML 解析与下游时间比较）

## 二、已验证

- 写临时 Node 脚本用 `toml@3.0.0`（插件实际依赖）解析所有 46 个 `.md` 文件的 TOML 头部（即从文件开头到首个 `[[...]]` 行之前的部分），**全部通过**：`HEAD-OK 46 / FAIL 0 / TOTAL 46`。脚本已删除。
- 整篇文件（保留 wikilink 与夹在中间的 `route` / `start_time`）不能被扁平的 TOML 解析器一次吃掉，这是**预期**——按 requirements §1 的设计，插件会以 `[[...]]` 为分段边界，每段单独解析 TOML。

## 三、未自动修复（需人工决策）

### 1. 文件名 vs 内部 `name` 不一致
- `涩谷唐吉可德.md`：文件名里写"唐吉可德"，但第 1 行 `name = "涩谷堂吉诃德"`、description 也写"堂吉诃德"；而 `日本之行.md` 中引用的是 `[[涩谷唐吉可德]]`（以文件名为准）。Wikilink 解析靠文件名，功能上不会断链，但显示名与文件名不符。
  - 建议：统一为"涩谷堂吉诃德"（标准译名），同时重命名文件 `涩谷唐吉可德.md → 涩谷堂吉诃德.md` 并更新 `日本之行.md` 里的引用。**不在本轮自动修复中处理**，需用户确认。

### 2. 缺失的子节点文件
- `银座.md` 引用了 `[[银座药局]]` 与 `[[银座奶茶]]`，但 `Roadmap/` 下不存在 `银座药局.md` / `银座奶茶.md`。
  - 建议：补创两份叶子地点文件，或从 `银座.md` 中删除这两行引用。需用户决定。

### 3. 其他重复 `[[...]]` 引用扫描结果
- 已扫描所有路线文件，除 `日本之行.md` 的秋叶原重复（已修）外，**未再发现**同文件内相邻或重复指向同一地点的 wikilink。

### 4. 日期格式一览（非错误，但列出以便 review）
以下文件使用"仅日期"形式（`YYYY-MM-DD`，非 `YYYY-MM-DD HH:mm:ss`），均为顶级路线的 `start_time` / `end_time`，语义是"某天"，符合 requirements 的格式约定，未改动：

- `日本之行.md`、`未来春游.md`、`访问成都.md`、`丽江大理昆明.md`、`青甘大环线.md`、`夸郎坡.md`、`京都奈良两日.md`、`成都美食两日.md`、`杭州西湖一日.md`、`北京一日.md`、`上海外滩一日.md`

以下文件没有时间字段（`待定探店.md`、`待定郊游.md`、`未计划周末.md`、`昆明长水国际机场.md`、`海埂大坝.md`、`青海湖.md`、`马来西亚国家博物馆.md`、`_day_20251102.md`），按约定允许缺省，未改动。

`_day_20251102.md` 仅有 `start_time = "2025-11-02"`（文件名像日记条目而非路线），是否纳入 roadmap 体系待确认。

## 四、风险与建议

1. **渲染入口语义**：本次移除了 7 个顶级路线文件的 `type`/`renders`，如果插件当前代码在加载顶级路线时**依赖** `type = "root"` 才能识别（即：以字段而非"roadmap.md 引用图"为入口判定），那么这些路线可能从 roadmapset 视图中消失。建议用户在 Obsidian 打开 `roadmap.md` 与任一被改过的路线做一次目视回归。
2. **`涩谷唐吉可德` 命名不一致**：一旦有基于 `name` 字段的显示或搜索，会产生认知混淆，建议尽快规范。
3. **`银座药局` / `银座奶茶` 缺文件**：Obsidian 会把它们显示成红色未创建链接，Roadmap 渲染时这两个节点可能表现为"空地点"或报错，取决于插件容错策略。
4. **`随机.md`** 之前的 `end_time = ""` 是否原本是占位符（代表"还没定"），若是，请考虑在数据规范中正式支持"允许缺省 end_time"（省略该键）以避免日后再次出现空字符串。
